# References

This folder contains the academic reference set underlying PAIciente's design.

- `references.md` is the human-readable annotated list.
- `references.bib` is the BibTeX export for citation managers.
- `evidence-map.csv` maps each design element to its supporting sources.
